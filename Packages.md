# Package Management

## 🔹 APT (Debian/Ubuntu)
- `sudo apt update`
- `sudo apt upgrade`
- `sudo apt install <package>`
- `sudo apt remove <package>`
- `sudo apt autoremove`

## 🔹 Snap
- `snap list`
- `sudo snap install <package>`